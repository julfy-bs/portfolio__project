.socials {
  display: flex;
}

.socials__item {
  margin-right: 40px;

  &:last-child {
    margin-right: 0;
  }
}

.socials__link {
  width: 25px;
  height: 25px;
  display: block;
}

.socials__icon {
  width: 100%;
  height: 100%;
  fill: currentColor;
  opacity: .35;

  &:hover {
    fill: #ff9a00;
    opacity: 1;
  }
}
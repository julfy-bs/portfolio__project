.avatar-img {
  max-width: 100%;
  max-height: 100%;
  display: block;
  vertical-align: middle;
  margin: auto;
  overflow: hidden;
}

.avatar {
  overflow: hidden;
  border-radius: 50%;
}

.user-info__avatar {
  width: 200px;
}